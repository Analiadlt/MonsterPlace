
const { NFTMARKETADDRESS, NFTADDRESS } = process.env;

  export const nftmarketaddress = `${NFTMARKETADDRESS}`
  export const nftaddress = `${NFTADDRESS}`
  //aca colocamos las direcciones que nos arroja hardhat una vez deployamos los contratos 
  