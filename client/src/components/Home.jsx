import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const Home = createSlice({
    name: sliceName,
    initialState,
    reducers: {
    
    }
});

export const {

} = Home.actions
export default Home.reducer